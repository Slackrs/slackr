

Welcome <%= @current_user.type %> with name <%= @current_user.name %>




<%= render "students/rostertotals" %>

<!-- <script>
  $(function() {
    $( "datepicker" ).click(function(e){
      e.preventDefault();
      var data = $('#id').serializeArray()[0];
      var value = data.value;
     
      document.location.href = "/cohorts/"
    })
  });
</script>
 -->
<form action="/cohorts/<%= params[:cohort_id] %>/attendances" method ="GET"class="date-picker">
  <textarea name="date" placeholder="enter date" id="datepicker">
  </textarea>
  <input type="hidden" class="create-card" name="cohort_id" value="<%= params[:cohort_id] %>">
  <input type="submit" value="go to date">
</form>


<% if @current_user.producer? %>

  <%= render "navs/producers" %>

  <script type="text/javascript">
   // $(this).ready(function(){
      $("#all-cohorts").hide();
      $(".cohort-view").hide();
   // });
  </script>

<% elsif @current_user.instructor? %>

  <script type="text/javascript">
   // $(this).on('click', ".cohort-view", function(){
      $(".cohort-view").hide();
   // });
  </script>


 
<% end %>

