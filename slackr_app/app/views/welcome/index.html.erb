<div class="container">
	<div class="row">
		<div class="col-xs-12">
			<%= form_tag do %>
				<%= text_field_tag(:date) %>
				<%= submit_tag("Submit", class: 'btn btn-sm btn-info') %>
			<% end %>
		</div>
	</div>
</div> 