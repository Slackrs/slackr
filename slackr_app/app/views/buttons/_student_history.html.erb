<!-- need controller#action for cohort_attendances_path join :students where student_id = ? -->
<% button_to "Student History", :controller => :my_controller, :action => :index, :id => "student-history" %>