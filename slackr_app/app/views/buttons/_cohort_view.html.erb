<% if @current_user.type == "Instructor" %>
<<<<<<< HEAD
	<%= button_to "Cohort View",  cohort_students_path(@current_user.cohort_id, @current_user.id), :class => "cohort-view" %>
=======
	<%= button_to "Cohort View", cohort_students_path(@current_user.cohort_id,@current_user.id), :class => "cohort-view" %>
>>>>>>> master
<% elsif @current_user.type == "Producer" %>
	<%= button_to "Cohort View", cohort_students_path(params[:id]), :class => "cohort-view"%>
<% else %>
<!-- this will be essentially a "back" button to redirect to student#show -->
	<%= button_to "Back", cohort_student_path(@current_user.cohort_id ,@current_user.id), :class => "back" %>
<% end %>