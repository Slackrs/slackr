<!-- this partial should basically be a calendar with search text_field, value => current date -->

<%= form_tag do %>
	<%= text_field_tag(:date) %>
	<%= submit_tag("Submit", class: 'btn btn-sm btn-info') %>

	<%= button_to "Today", edit_cohort_attendance_path(params[:cohort_id], Date.today) %>
<% end %>
